<?php

class Ext_Params
{
    const /* (char *) */ M_SUBDOMAIN  = 'subdomain'; // subdomain (used in edem)
    const /* (char *) */ M_DOMAIN     = 'domain';    // domain
    const /* (char *) */ M_VPORTAL    = 'vportal';   // vportal
    const /* (char *) */ M_PORT       = 'port';      // port
    const /* (char *) */ M_ID         = 'id';        // channel id
    const /* (char *) */ M_TOKEN      = 'token';     // token
    const /* (char *) */ M_LOGIN      = 'login';     // login
    const /* (char *) */ M_PASSWORD   = 'password';  // password
    const /* (char *) */ M_INT_ID     = 'int_id';    // internal id
    const /* (char *) */ M_HOST       = 'host';      // host
    const /* (char *) */ M_DEVICE_ID  = 'device';    // device (not yet used)
    const /* (char *) */ M_QUALITY_ID = 'quality';   // quality (used in viplime)
    const /* (char *) */ M_SERVER_ID  = 'server';    // server (used in sharavoz)
    const /* (char *) */ M_PROFILE_ID = 'profile';   // profile (used in sharavoz)
    const /* (char *) */ M_VAR1       = 'var1';      // custom regex capture group
    const /* (char *) */ M_VAR2       = 'var2';      // custom regex capture group
    const /* (char *) */ M_VAR3       = 'var3';      // custom regex capture group
}
