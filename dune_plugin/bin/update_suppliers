#!/bin/sh

grep -q newgui2021 /tmp/firmware_features.txt || exit 0

thisdir=`dirname $0`
basedir=`cd "$thisdir/.." && pwd`
plugin_name=`basename $basedir`

dirpath="/tmp/tv_app_suppliers"
filepath="$dirpath/$plugin_name"

[ -e "$dirpath" ] || mkdir "$dirpath"

